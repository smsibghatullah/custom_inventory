<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <record id="group_attendance_employee_selector" model="res.groups">
            <field name="name">Attendance Employee Selector</field>
            <field name="category_id" ref="base.module_category_human_resources"/>
            <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
            <field name="comment">
                User can see attendance only for employees selected on his user profile.
            </field>
        </record>

        <!-- Allow employee selection from all companies.
             This is read-only rule for internal users so the M2M field can list all employees. -->
        <record id="rule_hr_employee_all_companies_for_user_selection" model="ir.rule">
            <field name="name">MIR Employees visible for attendance selection - all companies</field>
            <field name="model_id" ref="hr.model_hr_employee"/>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <!-- Main attendance rule:
             For every internal user, only attendance of employees selected in res.users.employee_ids is visible.
             No company domain is used here. -->
        <record id="rule_hr_attendance_user_selected_employees" model="ir.rule">
            <field name="name">MIR Attendance by selected employees on user</field>
            <field name="model_id" ref="hr_attendance.model_hr_attendance"/>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="domain_force">[('employee_id', 'in', user.employee_ids.ids)]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>

    </data>
</odoo>
